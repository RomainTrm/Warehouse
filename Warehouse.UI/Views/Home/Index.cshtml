@model Warehouse.Domain.ReadModels.ItemsListView

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create new item", "Create")
</p>
<table class="table">
    <tr>
        <th>Name</th>
        <th>Quantity</th>
    </tr>

    @foreach (var item in Model.Items)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Units</td>
            <td>
                @Html.ActionLink("Details", "Details", new { id = item.Id.Value })
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Show disabled items", "DisabledItems")
</p>